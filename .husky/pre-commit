node_major="$(node -p 'process.versions.node.split(".")[0]')"

if [ "$node_major" -lt 22 ]; then
  echo "Node.js 22 or newer is required; currently using $(node -v)."
  exit 1
fi

node -v
pnpm -r --if-present lint
pnpm typecheck
