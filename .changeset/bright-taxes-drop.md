---
"@signal-kernel/async-runtime": minor
---

Add `createStreamResource` as the streaming sibling primitive of `createResource`, with support for progressive visible state, stable committed value, and interruption policies for cancellation and errors.
